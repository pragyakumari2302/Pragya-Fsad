# Server Configuration
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/adnan?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Maahie
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT Secret (In production, use an environment variable)
jwt.secret=4f6d8a39e8b7c3d2f1e4a7b9c6d8e2f1a3b5c7d9e2f4a6b8c0d2e4f6a8b0c2d4
jwt.expiration=86400000

# CORS Configuration
cors.allowed-origins=http://localhost:5173,http://localhost:5174,http://localhost:5175,http://localhost:3000

# Logging
logging.level.org.springframework.security=DEBUG
logging.level.com.adnan=DEBUG

# Disable Spring Boot DevTools automatic restart when running the packaged app
spring.devtools.restart.enabled=false
